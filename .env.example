# Copy to .env and fill in. .env is gitignored -- never commit real keys.

# Roboflow API key, used by scripts/download.py for `roboflow`-source datasets.
# Get yours from https://app.roboflow.com/settings/api, rotate immediately if
# one was ever pasted somewhere public (chat, issue, commit).
ROBOFLOW_API_KEY=
