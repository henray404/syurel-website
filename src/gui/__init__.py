"""Interactive model tester (Gradio).

    uv sync --extra gui        # then reinstall CUDA torch, see pyproject.toml
    uv run python -m gui.app

Lives on the feature/gui branch so the web stack never lands on a Pi install.
"""
