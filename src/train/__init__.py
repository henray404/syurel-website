"""Training: config-driven, single entry point, reproducible.

    python -m train.train --config configs/train/baseline.yaml

Self-checks (no data or GPU needed):

    python -m train.metrics
    python -m train.losses
"""
