"""Dataset conversion: heterogeneous public datasets -> one semantic-mask format.

Entry points (all runnable as modules):

    python -m data.convert            --dataset riptseg
    python -m data.water_pseudolabel  --dataset risid
    python -m data.review             --dataset risid --apply
    python -m data.splits
    python -m data.validate
"""
